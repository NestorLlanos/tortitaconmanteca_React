/* import { useContext } from "react"
import { CartContext } from "../../context/CartContext"

const Checkout = () =>{
    const {cart,total} = useContext(CartContex)


    const createOrder ={
        const objOrder = {
            comprador: {
                name: 'Nestor Llanos',
                email: 'nestordllanos@gmail.com',
                tel: '2616216213'
            },
            items: cart,
            total
        }
    }
    return (
        <div>
            <h1>Checkout</h1>
            <button onClick={createOrder}>Generar orden de compra</button>
        </div>
    )


}

export default Checkout  */